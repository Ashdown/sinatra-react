(function (React, App) {

    App.Components.Sample = React.createClass({
        render: function () {
            return (
                <div>
                    <h2>Todo app</h2>
                </div>
                );
        }

    });


})(React, App);